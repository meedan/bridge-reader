<% content_for :head do %>
<meta property="og:type"   content="article" />
<meta property="og:url"    content="<%= [BRIDGE_CONFIG['bridgembed_host'], 'medias', 'embed', @project, @collection, @item].join('/') %>" />
<%end%>
<%= render partial: 'medias/item', locals: { entry: @entries } %>
